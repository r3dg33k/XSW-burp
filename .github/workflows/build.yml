name: Build and Publish XSW-burp

on:
  push:
    branches:
      - main  # Runs when pushing to the 'main' branch
  pull_request:
    branches:
      - main  # Runs when a PR is created targeting the 'main' branch

jobs:
  build:
    runs-on: ubuntu-latest  # You can also use 'windows-latest' or 'macos-latest' if needed

    steps:
      # Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # Set up JDK 21
      - name: Set up JDK 21
        uses: actions/setup-java@v2
        with:
          java-version: '21'
          distribution: 'adoptopenjdk'

      # Cache Gradle dependencies (optional but speeds up builds)
      - name: Cache Gradle dependencies
        uses: actions/cache@v3
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      # Build the project using Gradle
      - name: Build with Gradle
        run: ./gradlew build

      # Optionally upload the build artifact (e.g., a .jar file)
      - name: Upload build artifact
        uses: actions/upload-artifact@v3
        with:
          name: burp-suite-extension
          path: build/libs/*.jar  # Change this if your artifact is located somewhere else
